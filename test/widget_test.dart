import 'package:flutter_test/flutter_test.dart';
import 'package:awe/main.dart';

void main() {
  test('main() exists and is callable', () {
    // Smoke test: verify the entry point compiles and can be referenced.
    expect(main, isA<Function>());
  });
}
